# Managed by chezmoi (system/nut/, mirrored by run_onchange_after_51-ups.sh.tmpl).
#
# standalone: the driver, upsd and upsmon all run here and upsd is bound to
# loopback. "netserver" would only be right if another host had to be told to
# shut down by this UPS — see docs/ups.md.
MODE=standalone
