gui:
  theme:
    lightTheme: false
    activeBorderColor:
      - "#c8a67e" # accent (warm gold)
      - bold
    inactiveBorderColor:
      - "#5a5752" # text-disabled
    optionsTextColor:
      - "#6fa3c7" # info (blue)
    selectedLineBgColor:
      - "#2c2c31" # surface-raised
    selectedRangeBgColor:
      - "#2c2c31" # surface-raised
    cherryPickedCommitBgColor:
      - "#7ab88a" # success (teal-green)
    cherryPickedCommitFgColor:
      - "#1a1a1e" # bg-solid
    unstagedChangesColor:
      - "#cf6a6a" # urgent (red)
    defaultFgColor:
      - "#e8e4df" # text-primary
