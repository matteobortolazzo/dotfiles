# Niri Mod+B on macOS: activate Zen Browser or launch it if closed.
cmd - b : open -a "Zen Browser"

# Close the focused window without sending macOS Quit to the app.
cmd - q : /opt/homebrew/bin/omniwmctl command close-focused-window
