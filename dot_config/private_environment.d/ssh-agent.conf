# Point the systemd user session (GUI apps, IDEs) at the 1Password SSH agent —
# the same one ~/.ssh/config uses via IdentityAgent. The previous value
# ($XDG_RUNTIME_DIR/ssh-agent.socket) referenced an ssh-agent user service
# that nothing ever started.
SSH_AUTH_SOCK=$HOME/.1password/agent.sock
